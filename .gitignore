# Android build outputs
android/.gradle/
android/build/
android/app/build/
android/local.properties

# IDE
.idea/
*.iml

# OS
.DS_Store
Thumbs.db

# Python
__pycache__/
*.pyc
.env
venv/
.venv/
.venv2x/
.venv3x/

# Cache / local data
.cache/
.depthai_cached_models/
.claude/

# Config with potential secrets
config.json

# Screenshots (dev only)
screenshot*.png
test_frame.*
test_live.*

# Conversion scripts (dev only)
convert_*.py
freecad_to_stl.py

# Model blobs (too large for git)
models/*.blob
# Exception: GridFront in-house V1 blob is small (~6 MB), tracked in repo
!models/gridfront-scout-v1.blob
# NN archives are derived from .blob + .json at runtime by
# pipeline.nn_archive_builder — never commit them.
models/*.tar.xz

# Temp
fix_network.ps1
runs/
training/downloads/
training/merged/
*.pt
.depthai_cached_models/

# Logs
*.log

# Flashed pipeline sidecars (rebuildable from source)
*.dap

# Ad-hoc debug screenshots
tablet_screenshot.png
